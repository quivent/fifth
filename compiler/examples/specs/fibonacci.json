{
  "word": "fibonacci",
  "description": "Calculates the nth Fibonacci number",
  "stack_effect": {
    "inputs": [
      {
        "name": "n",
        "type": "int",
        "constraint": "n >= 0"
      }
    ],
    "outputs": [
      {
        "name": "fib(n)",
        "type": "int",
        "value": "fib(n)"
      }
    ]
  },
  "properties": [
    "fibonacci(0) = 0",
    "fibonacci(1) = 1",
    "fibonacci(n) = fibonacci(n-1) + fibonacci(n-2) for n > 1"
  ],
  "test_cases": [
    {
      "description": "Base case: fib(0)",
      "input": [0],
      "output": [0],
      "tags": ["base_case"]
    },
    {
      "description": "Base case: fib(1)",
      "input": [1],
      "output": [1],
      "tags": ["base_case"]
    },
    {
      "description": "fib(2)",
      "input": [2],
      "output": [1],
      "tags": ["property"]
    },
    {
      "description": "fib(7)",
      "input": [7],
      "output": [13],
      "tags": ["property"]
    },
    {
      "description": "fib(10)",
      "input": [10],
      "output": [55],
      "tags": ["property"]
    },
    {
      "description": "fib(15)",
      "input": [15],
      "output": [610],
      "tags": ["edge_case"]
    }
  ],
  "complexity": {
    "time": "O(n)",
    "space": "O(1)"
  },
  "implementation": {
    "pattern": "ACCUMULATOR_LOOP_003",
    "hints": [
      "Use iterative approach with accumulators",
      "Avoid naive recursive implementation (exponential)",
      "Keep two accumulators for fib(n-1) and fib(n-2)"
    ]
  },
  "metadata": {
    "author": "FastForth Team",
    "version": "1.0.0",
    "created": "2025-01-14T00:00:00Z",
    "tags": ["math", "sequence", "iterative"]
  }
}
